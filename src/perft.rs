use crate::board::Board;
use crate::move_generator::{MoveList, Move};

// pub fn perft(fen: &str, depth: u32) -> {
//     let mut board = Board::new();
//     board.read_fen(fen);
//     let mut move_list = MoveList::new(&board);
// }